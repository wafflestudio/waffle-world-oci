apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: allclear-valkey
  labels:
    app: allclear-valkey
spec:
  serviceName: allclear-valkey
  replicas: 1
  selector:
    matchLabels:
      app: allclear-valkey
  template:
    metadata:
      labels:
        app: allclear-valkey
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      serviceAccountName: allclear
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
        - name: valkey
          image: docker.io/valkey/valkey:9-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 25m
              memory: 128Mi
            limits:
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: allclear-valkey
spec:
  type: ClusterIP
  selector:
    app: allclear-valkey
  ports:
    - port: 6379
      targetPort: 6379
