apiVersion: apps/v1
kind: Deployment
metadata:
  name: allclear-web
  labels:
    app: allclear-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: allclear-web
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  revisionHistoryLimit: 4
  template:
    metadata:
      labels:
        app: allclear-web
    spec:
      serviceAccountName: allclear
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
        - image: yny.ocir.io/ax1dvc8vmenm/allclear-dev/allclear-web:latest
          name: allclear-web
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 256Mi
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health-check
              port: 8080
          readinessProbe:
            httpGet:
              path: /health-check
              port: 8080
            timeoutSeconds: 3
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /health-check
              port: 8080
            failureThreshold: 20
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: allclear
---
apiVersion: v1
kind: Service
metadata:
  name: allclear-web
spec:
  type: ClusterIP
  selector:
    app: allclear-web
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: allclear-web
spec:
  gateways:
    - istio-ingress/waffle-ingressgateway
    - mesh
  hosts:
    - allclear-web.allclear-dev.svc.cluster.local
    - allclear-dev.wafflestudio.com
  http:
    - route:
        - destination:
            host: allclear-web
