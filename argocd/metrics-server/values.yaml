args:
# OKE compatibility: kubelet certificates are not always signed by a CA trusted
# by metrics-server, so TLS verification is disabled for kubelet scraping.
- --kubelet-insecure-tls

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
