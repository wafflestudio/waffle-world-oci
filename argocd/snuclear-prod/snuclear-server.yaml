apiVersion: apps/v1
kind: Deployment
metadata:
  name: snuclear-server
  labels:
    app: snuclear-server
spec:
  # replicas: HPA
  selector:
    matchLabels:
      app: snuclear-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  revisionHistoryLimit: 4
  template:
    metadata:
      labels:
        app: snuclear-server
    spec:
      serviceAccountName: snuclear-server-prod
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
        - image: yny.ocir.io/ax1dvc8vmenm/snuclear-prod/snuclear-server:29
          name: snuclear-server
          resources:
            requests:
              cpu: 50m
              memory: 384Mi
            limits:
              memory: 768Mi
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8080
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 8080
            timeoutSeconds: 3
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /api/v1/health
              port: 8080
            failureThreshold: 20
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: snuclear-server-hpa
  labels:
    app: snuclear-server
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: snuclear-server
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: snuclear-server-prod
---
apiVersion: v1
kind: Service
metadata:
  name: snuclear-server
spec:
  type: ClusterIP
  selector:
    app: snuclear-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: snuclear-server
spec:
  gateways:
    - istio-ingress/waffle-ingressgateway
    - mesh
  hosts:
    - snuclear-server.wafflestudio.com
  http:
    # 액추에이터 메트릭 경로는 외부 게이트웨이에서 차단(404).
    # 클러스터 내부 Prometheus는 Service(pod)로 직접 scrape하므로 mesh 트래픽엔 영향 없음.
    - match:
        - uri:
            prefix: /api/v1/prometheus
          gateways:
            - istio-ingress/waffle-ingressgateway
        - uri:
            prefix: /api/v1/metrics
          gateways:
            - istio-ingress/waffle-ingressgateway
      directResponse:
        status: 404
    - route:
        - destination:
            host: snuclear-server
      headers:
        request:
          set:
            X-Forwarded-Proto: "https"
