apiVersion: apps/v1
kind: Deployment
metadata:
  name: siksha-api-server
  labels:
    app: siksha-api-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: siksha-api-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  revisionHistoryLimit: 4
  template:
    metadata:
      labels:
        app: siksha-api-server
    spec:
      serviceAccountName: siksha
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
        - image: yny.ocir.io/ax1dvc8vmenm/siksha-prod/siksha-api-server:26
          name: siksha-api-server
          resources:
            requests:
              cpu: 200m
              memory: 192Mi
            limits:
              memory: 192Mi
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: siksha
imagePullSecrets:
  - name: ocir-pull-secret
---
apiVersion: v1
kind: Service
metadata:
  name: siksha-api-server
spec:
  type: ClusterIP
  selector:
    app: siksha-api-server
  ports:
    - port: 80
      targetPort: 8000
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: siksha-api-server
spec:
  gateways:
    - istio-ingress/waffle-ingressgateway
    - mesh
  hosts:
    - siksha-api.wafflestudio.com
  http:
    - route:
        - destination:
            host: siksha-api-server
      headers:
        request:
          set:
            X-Forwarded-Proto: "https"
