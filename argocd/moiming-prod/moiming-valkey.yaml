apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: moiming-valkey
  labels:
    app: moiming-valkey
spec:
  serviceName: moiming-valkey
  replicas: 1
  selector:
    matchLabels:
      app: moiming-valkey
  template:
    metadata:
      labels:
        app: moiming-valkey
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      serviceAccountName: moiming-server-prod
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
        - name: valkey
          image: docker.io/valkey/valkey:9-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: moiming-valkey
spec:
  type: ClusterIP
  selector:
    app: moiming-valkey
  ports:
    - port: 6379
      targetPort: 6379
